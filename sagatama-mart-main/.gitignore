# Dependencies
node_modules/

# Environment variables — JANGAN PERNAH commit file .env asli
.env
.env.local
.env.*.local

# Firebase / Google service account keys (private key di dalamnya!)
*-key.json
*serviceAccount*.json
portal-sagatama-key.json
hidayatulamin-key.json

# Hasil generate dari script migrasi/verifikasi — berisi data saldo user,
# tidak perlu masuk repo
migration-report.json
verify-report.json

# Vercel
.vercel

# OS / editor
.DS_Store
Thumbs.db
